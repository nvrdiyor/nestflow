# NestFlow AI — VPS deployment.
#
#   IP-only (http://SERVER_IP):
#     docker compose up -d --build
#
#   With a domain + automatic HTTPS (in .env set DOMAIN=your-domain.com and
#   HTTP_PORT=127.0.0.1:8787 so Caddy owns ports 80/443):
#     docker compose --profile https up -d --build
#
# Copy .env.deploy.example to .env and set ADMIN_PASSWORD (required).

services:
  nestflow:
    build: .
    restart: unless-stopped
    environment:
      ADMIN_USERNAME: ${ADMIN_USERNAME:-nvrdiyor}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:?Set ADMIN_PASSWORD in .env}
      JWT_SECRET: ${JWT_SECRET:-}
      STARTING_CREDITS: ${STARTING_CREDITS:-100}
      # Keep false in IP-only mode; set TRUST_PROXY=1 in .env when using the
      # https profile (Caddy is then the single proxy hop in front).
      TRUST_PROXY: ${TRUST_PROXY:-false}
      CORS_ORIGIN: ${CORS_ORIGIN:-}
    volumes:
      - nestflow-data:/app/apps/api/data
    ports:
      - "${HTTP_PORT:-80}:8787"

  caddy:
    image: caddy:2-alpine
    profiles: ["https"]
    restart: unless-stopped
    depends_on:
      - nestflow
    environment:
      # No ${DOMAIN:?} here: compose interpolates EVERY service even when its
      # profile is inactive, which would break plain `docker compose up`.
      # The guard in `command` enforces DOMAIN when the https profile runs.
      DOMAIN: ${DOMAIN:-}
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - caddy-data:/data
      - caddy-config:/config
    # Site block for the domain + a catch-all that redirects plain-http (incl.
    # bare-IP links) to the canonical https origin.
    command: sh -c '[ -n "$$DOMAIN" ] || { echo "Set DOMAIN in .env for HTTPS" >&2; exit 1; }; printf "%s {\n  reverse_proxy nestflow:8787\n}\nhttp:// {\n  redir https://%s{uri} permanent\n}\n" "$$DOMAIN" "$$DOMAIN" > /etc/caddy/Caddyfile && caddy run --config /etc/caddy/Caddyfile'

volumes:
  nestflow-data:
  caddy-data:
  caddy-config:
